<!DOCTYPE html>
<html>
  <head>
  <title><%= content_for(:title) || "PuntoClave - Predicciones de Tenis con IA" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Bootstrap CSS %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag 'puntoclave', media: 'all', 'data-turbo-track': 'reload' %>
    <%= javascript_importmap_tags %>
    
    <%# Bootstrap JS %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
        <div class="container">
          <a class="navbar-brand d-flex align-items-center" href="/">
            <%= image_tag asset_path('logo.png'), alt: 'PuntoClave', width: 140, height: 36, class: 'me-2' %>
          </a>
        <div class="navbar-nav ms-auto d-flex align-items-center gap-3">
          <%= link_to predictions_path, class: 'nav-link d-flex align-items-center' do %>
            <%= image_tag asset_path('predicciones.png'), alt: 'Predicciones', height: 20, class: 'me-1 nav-icon' %>
            <span>Predicciones</span>
          <% end if defined?(predictions_path) rescue nil %>

          <%= link_to players_path, class: 'nav-link d-flex align-items-center' do %>
            <%= image_tag asset_path('jugadores.png'), alt: 'Jugadores', height: 20, class: 'me-1 nav-icon' %>
            <span>Jugadores</span>
          <% end if defined?(players_path) rescue nil %>

          <%= link_to matches_path, class: 'nav-link d-flex align-items-center' do %>
            <%= image_tag asset_path('partidos.png'), alt: 'Partidos', height: 20, class: 'me-1 nav-icon' %>
            <span>Partidos</span>
          <% end if defined?(matches_path) rescue nil %>
        </div>
      </div>
    </nav>

    <!-- Hero removed as requested -->

    <main class="container my-4">
      <% if flash[:success] %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= flash[:success] %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      
      <% if flash[:alert] %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= flash[:alert] %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
